server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Handle Single Page Application routing
    location / {
        try_files $uri $uri/ /index.html;
    }
    # Note: no proxy to backend here – frontend will call backend directly via VITE_API_URL
}
